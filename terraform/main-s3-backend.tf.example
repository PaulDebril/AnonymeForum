terraform {
  backend "s3" {
    bucket         = "your-terraform-state-bucket"  # Créez ce bucket
    key            = "anonyme-forum/terraform.tfstate"
    region         = "eu-central-1"
    dynamodb_table = "terraform-state-locks"        # Pour le verrouillage
    encrypt        = true
  }
  
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 4.0"
    }
    tls = {
      source  = "hashicorp/tls"
      version = "~> 4.0"
    }
    local = {
      source  = "hashicorp/local"
      version = "~> 2.0"
    }
  }
}

provider "aws" {
  region = "eu-central-1"
}